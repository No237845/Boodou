# Copier en .env (ou .env.local, ignoré par git).
# URL du serveur Boodou, sans slash final.
# En développement : l'adresse IP de la machine qui lance uvicorn, sur le même
# Wi-Fi que le téléphone. Jamais "localhost" (ce serait le téléphone lui-même).
EXPO_PUBLIC_API_URL=http://192.168.1.10:8000
